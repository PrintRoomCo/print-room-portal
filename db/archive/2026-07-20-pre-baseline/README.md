# Pre-baseline migration archive — 2026-07-20 (customer portal)

The 11 migration files here were `print-room-portal`'s local
`supabase/migrations/` before the 2026-07-20 schema baseline reconciliation.

They were **all already applied** to the shared Supabase project
(`bthsxgmcnbvwwgvdveek`) — verified against live schema on 2026-07-20 — so their
effects are captured in the schema baseline now owned by
`print-room-staff-portal`. They are kept here for provenance only and must **not**
be re-run.

Going forward this repo holds **no** migrations; `print-room-staff-portal` owns
the schema. See `../../../supabase/migrations/README.md`.
