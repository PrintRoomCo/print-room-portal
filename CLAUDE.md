@AGENTS.md

## Implementation plans

Plans in this repo are written for **Jon to implement by hand**, not for an agent.
Use the `human-executed-plans` skill when writing one (it overrides
`superpowers:writing-plans`): tests complete and verbatim, implementation given as
signature + invariants + traps, a `Why this shape` / `Rejected` note per task.
